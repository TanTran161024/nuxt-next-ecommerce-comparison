import Image from 'next/image'
import Link from 'next/link'
import type { Category, ProductListItem } from '../../types/product'
import { CartButton } from '../cart/CartButton'
const money=(n:number)=>new Intl.NumberFormat('vi-VN',{style:'currency',currency:'VND',maximumFractionDigits:0}).format(n)
export function ProductCard({product,categories}:{product:ProductListItem;categories:Category[]}){const label=categories.find(x=>x.value===product.category)?.label??product.category;return <article className="flex h-full flex-col overflow-hidden rounded-[var(--radius-md)] border border-[var(--color-border)] bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><Link className="group flex flex-1 flex-col focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--color-focus)]" href={`/product/${product.slug}`}><Image src={product.image} alt={product.imageAlt} width={800} height={600} className="aspect-[4/3] w-full bg-[var(--color-surface-muted)] object-cover"/><div className="flex flex-1 flex-col space-y-2 p-4"><div className="flex items-start justify-between gap-3 text-xs font-bold text-[var(--color-primary)]"><span>{product.brand}</span>{product.onSale&&<span className="rounded-full bg-amber-100 px-2 py-1 text-[var(--color-sale)]">Giảm giá</span>}</div><h3 className="line-clamp-2 min-h-12 font-semibold text-[var(--color-text)] group-hover:text-[var(--color-primary)]">{product.name}</h3><p className="text-sm text-[var(--color-text-muted)]"><span className="rounded-full bg-[var(--color-surface-muted)] px-2 py-1 text-xs">{label}</span><span className="ml-1">{product.color}</span></p><p className="mt-auto font-bold text-[var(--color-text)]">{money(product.price)} {product.originalPrice!==null&&<del className="ml-2 font-normal text-[var(--color-text-muted)]">{money(product.originalPrice)}</del>}</p></div></Link><CartButton product={{productId:product.id,slug:product.slug,name:product.name,brand:product.brand,image:product.image,imageAlt:product.imageAlt,price:product.price}} /></article>}
