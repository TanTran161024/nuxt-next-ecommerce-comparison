'use client'
import { useRouter, useSearchParams } from 'next/navigation'
import type { Brand, Category } from '../../types/product'
export function ProductFilters({brands,categories,colors}:{brands:Brand[];categories:Category[];colors:string[]}){const router=useRouter();const params=useSearchParams();const apply=(form:FormData)=>{const q=new URLSearchParams();for(const key of ['brand','category','color','minPrice','maxPrice']){const value=form.get(key);if(typeof value==='string'&&value.trim())q.set(key,value.trim())}router.push(`/products${q.size?`?${q}`:''}`)};return <form action={apply} className="ui-card grid gap-4 p-4 sm:grid-cols-2 lg:grid-cols-1"><div><label className="mb-1 block text-sm font-semibold" htmlFor="brand">Thương hiệu</label><select id="brand" name="brand" defaultValue={params.get('brand')??''} className="w-full rounded-[var(--radius-sm)] border border-[var(--color-border)] bg-white px-3"><option value="">Tất cả thương hiệu</option>{brands.map((brand)=><option key={brand.value} value={brand.value}>{brand.label}</option>)}</select></div><div><label className="mb-1 block text-sm font-semibold" htmlFor="category">Loại sản phẩm</label><select id="category" name="category" defaultValue={params.get('category')??''} className="w-full rounded-[var(--radius-sm)] border border-[var(--color-border)] bg-white px-3"><option value="">Tất cả loại sản phẩm</option>{categories.map((category)=><option key={category.value} value={category.value}>{category.label}</option>)}</select></div><div><label className="mb-1 block text-sm font-semibold" htmlFor="color">Màu sắc</label><select id="color" name="color" defaultValue={params.get('color')??''} className="w-full rounded-[var(--radius-sm)] border border-[var(--color-border)] bg-white px-3"><option value="">Tất cả màu sắc</option>{colors.map((color)=><option key={color} value={color}>{color}</option>)}</select></div><div className="grid grid-cols-2 gap-3 sm:col-span-2 lg:col-span-1"><div><label className="mb-1 block text-sm font-semibold" htmlFor="minPrice">Giá tối thiểu</label><input id="minPrice" name="minPrice" type="number" min="0" defaultValue={params.get('minPrice')??''} className="w-full rounded-[var(--radius-sm)] border border-[var(--color-border)] px-3"/></div><div><label className="mb-1 block text-sm font-semibold" htmlFor="maxPrice">Giá tối đa</label><input id="maxPrice" name="maxPrice" type="number" min="0" defaultValue={params.get('maxPrice')??''} className="w-full rounded-[var(--radius-sm)] border border-[var(--color-border)] px-3"/></div></div><div className="flex gap-2 sm:col-span-2 lg:col-span-1"><button className="rounded-[var(--radius-sm)] bg-[var(--color-primary)] px-4 py-2 text-sm font-semibold text-white hover:bg-[var(--color-primary-hover)]">Áp dụng</button><button type="button" onClick={()=>router.push('/products')} className="rounded-[var(--radius-sm)] border border-[var(--color-border)] px-4 py-2 text-sm font-semibold hover:bg-[var(--color-surface-muted)]">Đặt lại</button></div></form>}
