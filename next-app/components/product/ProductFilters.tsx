'use client'
import { useRouter, useSearchParams } from 'next/navigation'; import type { Brand, Category } from '../../types/product'
export function ProductFilters({brands,categories,colors}:{brands:Brand[];categories:Category[];colors:string[]}){const router=useRouter();const params=useSearchParams();const apply=(form:FormData)=>{const q=new URLSearchParams();for(const k of ['brand','category','color','minPrice','maxPrice']){const v=form.get(k);if(typeof v==='string'&&v.trim())q.set(k,v.trim())}router.push(`/products${q.size?`?${q}`:''}`)};return <form action={apply} className="grid gap-3 rounded-xl border bg-white p-4 md:grid-cols-3"><select name="brand" defaultValue={params.get('brand')??''}><option value="">Tất cả thương hiệu</option>{brands.map(x=><option key={x.value} value={x.value}>{x.label}</option>)}</select><select name="category" defaultValue={params.get('category')??''}><option value="">Tất cả loại</option>{categories.map(x=><option key={x.value} value={x.value}>{x.label}</option>)}</select><select name="color" defaultValue={params.get('color')??''}><option value="">Tất cả màu</option>{colors.map(x=><option key={x}>{x}</option>)}</select><input name="minPrice" type="number" min="0" placeholder="Giá tối thiểu" defaultValue={params.get('minPrice')??''}/><input name="maxPrice" type="number" min="0" placeholder="Giá tối đa" defaultValue={params.get('maxPrice')??''}/><div className="flex gap-2"><button className="rounded bg-slate-950 px-4 py-2 text-white">Áp dụng</button><button type="button" onClick={()=>router.push('/products')} className="rounded border px-4 py-2">Đặt lại</button></div></form>}
