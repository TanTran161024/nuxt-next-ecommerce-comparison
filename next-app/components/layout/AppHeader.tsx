'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useEffect, useState } from 'react'
import { CartLink } from '../cart/CartButton'
export function AppHeader(){const [open,setOpen]=useState(false);const pathname=usePathname();useEffect(()=>{const close=(event:KeyboardEvent)=>{if(event.key==='Escape')setOpen(false)};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);const nav=(mobile=false)=><>{[['/','Trang chủ'],['/products','Sản phẩm']].map(([href,label])=><Link className={`${mobile?'px-3 py-3':'px-4 py-2'} rounded-[var(--radius-sm)] text-sm font-semibold ${pathname===href?'bg-[var(--color-surface-muted)] text-[var(--color-primary)]':'text-[var(--color-text-muted)] hover:text-[var(--color-text)]'}`} href={href} key={href} onClick={()=>setOpen(false)}>{label}</Link>)}</>;return <header className="sticky top-0 z-40 border-b border-[var(--color-border)] bg-white/95 shadow-sm backdrop-blur"><div className="site-container flex h-[var(--header-height)] items-center justify-between gap-3"><Link className="text-base font-black tracking-tight text-[var(--color-text)] sm:text-lg" href="/">Bước Chân Demo</Link><nav className="hidden items-center gap-1 md:flex" aria-label="Điều hướng chính">{nav()}</nav><div className="flex items-center gap-2"><CartLink/><button aria-controls="mobile-menu" aria-expanded={open} aria-label="Mở menu" className="inline-flex rounded-[var(--radius-sm)] border border-[var(--color-border)] p-2 md:hidden" type="button" onClick={()=>setOpen(value=>!value)}><svg aria-hidden="true" className="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M4 7h16M4 12h16M4 17h16" strokeLinecap="round" strokeWidth="1.8"/></svg></button></div></div>{open&&<nav id="mobile-menu" className="border-t border-[var(--color-border)] bg-white p-3 md:hidden" aria-label="Điều hướng di động"><div className="site-container grid gap-1">{nav(true)}<Link className="rounded px-3 py-3 text-sm font-semibold text-[var(--color-text-muted)] hover:bg-[var(--color-surface-muted)]" href="/cart" onClick={()=>setOpen(false)}>Giỏ hàng</Link></div></nav>}</header>}
